[**threshold-elgamal**](../../index.md)

***

[threshold-elgamal](../../modules.md) / [threshold-elgamal](../index.md) / concatBytes

# Function: concatBytes()

> **concatBytes**(...`arrays`): `Uint8Array`

Concatenates multiple byte arrays into a single contiguous buffer.

## Parameters

### arrays

...`Uint8Array`\<`ArrayBufferLike`\>[]

Byte arrays to concatenate in order.

## Returns

`Uint8Array`

A single `Uint8Array` containing every input array.
