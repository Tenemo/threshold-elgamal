# Getting started

Use the safe root package. The current public surface is centered on additive ElGamal, while threshold sharing, proofs, transport, protocol helpers, and DKG reducers live under narrower subpaths.

`README.md` is the canonical onboarding document for installation, runtime prerequisites, the safe additive quickstart, and additive bound planning.

Before using the typed protocol payloads documented in this site, confirm that your installed package version is `2.x`. The legacy `0.1.x` line does not expose the same surface.

## Where to go next

- For installation, runtime expectations, and the root-package quickstart, start with [README.md](../../README.md).
- For tallying sums, read [Additive ElGamal](additive-elgamal.html).
- For the supported protocol path, read [Three-participant voting flow](three-participant-voting-flow.html).
- For group selection, scalar rules, encoding expectations, and validation errors, read [Groups and validation](groups-and-validation.html).
- For exact signatures and exported types, use the [API reference](../api/index.html).
