# API reference

This section is the generated reference for the current package surface.

Use the generated pages as a type-and-contract reference after reading the manual guides. They are not meant to replace the guides or spec pages.

## Public module map

- `threshold-elgamal`: safe root package for groups, additive ElGamal, validation helpers, and encoding helpers
- `core`: finite-field arithmetic, group definitions, randomness, validation, and exported error classes
- `elgamal`: additive ElGamal helpers, bounded discrete-log recovery helpers, and key generation
- `serialize`: encoding helpers used by the current public API and later proof-oriented transcript work

## How to read this reference

- Start with the safe root package unless you need a specific subpath import.
- Use the docs portal for module entry pages, guides, and security context.
